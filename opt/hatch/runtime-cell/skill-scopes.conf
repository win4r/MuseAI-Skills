# skill-scopes.conf -- GENERATED from extensions_scoped.toml. DO NOT EDIT.
#
# extensions_scoped.toml is the single source of truth: each [[skills]] entry
# lists the skill's `scopes`, and this file is derived from them -- one
# `<scope> <directory> ...` line per scope, both sorted for a stable render.
#
# The tokens after the scope are skill DIRECTORY names, not SKILL.md names:
# launch-daemon.sh resolves each against /opt/hatch/skills-scoped/<directory>.
#
# launch-daemon.sh reads this at cell boot and reveals only the gated skills
# whose scope matches the VM's JARVIS_CD_CHANNEL. An unset/unknown channel or a
# missing line reveals nothing (fail-closed). Edit the manifest, not this file.
canary box canva dropbox klaviyo meta-ads quickbooks shopify square whatsapp
external-connectors-test canva dropbox klaviyo quickbooks
hatch-e2e health meta-ads nutrition social-competitor-analysis social-content-performance
hatch-evals facebook-cli-internal health meta-ads nutrition social-competitor-analysis social-content-performance
hatch-pro box canva dropbox klaviyo meta-ads quickbooks social-competitor-analysis social-content-performance
hatch-pro-internal asana box canva dropbox evernote facebook-cli-internal klaviyo linear lovable meta-ads quickbooks replit slack social-competitor-analysis social-content-performance vercel zapier zoom
internal-test arlo asana audio_notes_read canva documents dropbox evernote facebook-cli-internal health instacart klaviyo linear lovable meta-ads nutrition polymarket price-tracker quickbooks replit ring shopify slack social-competitor-analysis social-content-performance square vercel zapier zoom
prod box canva dropbox klaviyo meta-ads quickbooks shopify square whatsapp
quail box canva dropbox klaviyo meta-ads quickbooks shopify square whatsapp
shoppify-test shopify
